# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Telegram User Client (for large file uploads via Telethon - Optional)
# Get these from https://my.telegram.org/apps
# Required only for uploading files larger than 50MB (up to 2GB)
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PHONE=

# Database Configuration (handled by docker-compose)
# DATABASE_URL=postgresql://botuser:botpassword@postgres:5432/botdb

# Redis Configuration (handled by docker-compose)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=

# Application Settings
MAX_CONCURRENT_DOWNLOADS=5
DOWNLOAD_TIMEOUT=300
TEMP_DOWNLOAD_PATH=/app/downloads

# Cookie Refresher
# Set to true to automatically refresh YouTube cookies every minute
# Set to false to only allow manual refresh via /refresh command
AUTO_REFRESH_COOKIES=true
